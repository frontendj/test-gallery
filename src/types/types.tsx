export type FetchedImage = {
    author: string;
    download_url: string;
    height: number;
    id: string;
    url: string;
    width: number;
};

export type Page = number;
export type Limit = number;
