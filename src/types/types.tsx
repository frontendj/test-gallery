export type Global = {
    isSomethingGoingOn?: boolean;
};

export type FetchedImage = {
    author: string;
    download_url: string;
    height: number;
    id: string;
    url: string;
    width: number;
};
